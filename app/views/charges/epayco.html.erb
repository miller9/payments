<script>
  var handler = ePayco.checkout.configure({
    key: '40ed9b66a2f57cad918e7f7dac23b9ca',
    //test: true     //test: <%= Rails.env.production? ? false : true %>
    test: true
  })
  var data = {
    //Parametros compra (obligatorio)
    name: "PRUEBAS de tienda online",
    description: "Pago de prueba -ePayco- para online store",
    invoice: "<%= @charge.uid %>",
    currency: "cop",
    amount: "<%= @charge.amount %>",
    tax_base: "0",
    tax: "0",
    country: "co",
    lang: "en",

    // Depending on Choosed option: Onpage="false" - Standard="true"
    external: "true",

    confirmation: "http://localhost:3000/epayco/confirmation",
    response: "http://localhost:3000/epayco/response",

    //atributo deshabilitación metodo de pago
    methodsDisable: ["TDC", "PSE", "SP", "CASH", "DP"]

  };

  handler.open(data);
</script>
